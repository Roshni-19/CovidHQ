<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covidhq">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CovidHQ">
        <activity android:name=".LiveTV"></activity>
        <activity android:name=".DoctorsActivity" />
        <activity android:name=".DietPlan" />
        <activity android:name=".Oxygen" />
        <activity android:name=".SiddhaMedication" />
        <activity android:name=".AyurvedaMedication" />
        <activity android:name=".AllopathyMedication" />
        <activity
            android:name=".Medications"
            android:label="@string/title_activity_medications"
            android:theme="@style/Theme.CovidHQ.NoActionBar" />
        <activity android:name=".Symptoms" />
        <activity android:name=".GeneralGuideLines" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>